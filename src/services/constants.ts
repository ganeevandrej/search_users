export enum SortRotation {
    DESC = "desc",
    ASC = "asc"
}

export const initialDetailsUser = {
    name: "",
    followers: 0,
    following: 0,
    publicRepos: 0
}

export const DEFAULT_PAGE: number = 1;
export const DEFAULT_QUERY: string = " ";